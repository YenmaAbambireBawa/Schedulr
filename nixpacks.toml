[phases.build]
cmds = ["composer install --no-dev"]

[phases.setup]
aptPkgs = ["php8.2", "php8.2-mysql", "php8.2-pdo", "php8.2-mbstring", "php8.2-xml", "php8.2-curl"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
