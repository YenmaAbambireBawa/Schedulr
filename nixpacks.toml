[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.xml", "php82Extensions.curl"]

[phases.build]
cmds = ["composer install --no-dev"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
